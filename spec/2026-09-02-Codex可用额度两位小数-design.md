# Codex 可用额度两位小数

## 目标

Codex 展开面板中的“可用额度”固定显示两位小数，例如 `500.00`、`12.50`。

## 范围

- 只处理 `credits` 展示值，不修改 Codex 原始额度和计算逻辑。
- 数值超过两位小数时按标准规则四舍五入。
- `available`、`unlimited` 等非数值状态保持原有本地化展示。
- Claude、WorkBuddy、DeepSeek 的额度展示不受影响。

## 验证方式

- 单元测试覆盖整数、一位小数、超过两位小数和非数值状态。
- 运行完整测试并重新打包应用。
